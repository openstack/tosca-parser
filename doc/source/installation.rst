============
Installation
============

At the command line::

    $ pip install heat-translator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv heat-translator
    $ pip install heat-translator